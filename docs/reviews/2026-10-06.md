# Review — 2026-10-06 (Analyzer)

Scope: **PRs #167–#174** — the full batch I specced (**FOF-UNITS**, **FOF-OVERLAYS**, **RENDER-SUITE-GREEN-3**,
**PAN-RELEASE-PLAYBACK**, **RENDER-IDLE-QUIET**, **SETTLED-BOARD-INVARIANT**) plus a **same-day owner
playtest** of the FoF work (three bugs + a ring weight, and FOF-LOCAL, all fixed in-cycle), MODEL-PRELOAD
(fetch the enemy's models too), the value-budget/rim-light render pass, prop variety, and CHARACTER_PLAYBOOK.
Plus the Builder's session-17 Open Questions. **No Dev Notes** — a review cycle. Base: `origin/main`
(`6d5b3db`).

## Verification

- `npm test` → **3292 passing** (1955 + 1035 + 302). `npm run typecheck` clean. Engine purity grep clean.
  **`packages/engine/src` is untouched since my last review** (empty diff) — every change is `packages/client`,
  so phase order, dash immunity, determinism and N-safety are not at issue engine-side (OQ #5 confirmed).
- **FOF-UNITS + FOF-OVERLAYS — shipped, playtested, and hardened in one cycle.** Colour is now resolved from
  the viewer's seat (self blue / ally green / foe red on units; friendly-blue committed overlays). Ruled in
  edge-cases (FOF-COLORS, now RULED). The owner playtested it same-day and the three bugs it surfaced were
  each **root-caused to a shape, not patched to a symptom**, with regression tests **verified against the
  unfixed code**: the lock-in green flip (`seatIdx` past the seat list → a `?? 0` empty-set viewer that
  demoted the own team to ally — "a wrong answer wearing a default's clothes"; fixed by falling back to the
  last seat on the clock), the white bars (colour lives in `color` **and** `emissive` on a glowing material;
  only one was repainted), and the bulky ring (0.12→a third of the weight). **FOF-LOCAL** then settled the
  hot-seat identity — self is the *person*, so a hot-seat's whole team is blue and green is a networked-only
  ally. All folded into the ruling.
- **PAN-RELEASE-PLAYBACK** — a planning pan releases on resolution so playback follows the action; orbit/zoom
  persist. As specced.
- **RENDER-IDLE-QUIET + SETTLED-BOARD-INVARIANT** — the idle board is quiet and pinned by a named test, the
  app-side half RENDER-ON-DEMAND was built for. Good.
- **RENDER-SUITE-GREEN-3** — took the bulk of the map-rebuild regressions (re-pointed the 4v4 boots to a map
  that has 4v4, the fixed-point sweeps to rings, the colour predicates to viewer-relative FoF). **Three
  tests remain**, each left as a spec choice for the Analyzer — see OQ #3/#4 and RENDER-SUITE-GREEN-4.

## Done well (one line)

The FoF playtest fixes are the model of root-causing over patching: every one was written up as a reusable
*shape* — a defaulted empty-set viewer, colour-in-two-properties on a glowing material, a ring that became a
floor tile — and each shipped a regression test proven to fail on the unfixed code, so none of them can
quietly come back.

## Builder Open Questions — session-17 resolution

| # | Question | Resolution |
|---|---|---|
| 1 | FOF-UNITS' **model outline** did not ship (ring + nameplate carry the read; the body doesn't take the FoF colour — `emissive` is the flash's, an inverted hull is per-mesh geometry on a slow path) | **Accept as shipped for the core; spec FOF-OUTLINE (follow-up).** Ring + nameplate solve the mirror problem the owner asked for. The clean technique for the outline is **tint the achromatic rim light** (shipped in `5e022a8`) by FoF colour — no new geometry, no `emissive` conflict. Med priority; confirm in the next playtest whether ring+nameplate is already enough before investing. |
| 2 | The friendly overlay blue is the **same hue as `REACH`** (range) — separated by weight, not hue; now a live overlap | **Designer call, flagged (FOF-OVERLAY-HUE).** The reconciliation the item took (own live aim stays meaning-coded; only committed/telegraphed overlays take FoF colour) stands; if friendly-blue vs range-blue still reads ambiguous on a real board with an ally's committed plan under a live aim, the Designer picks a distinct friendly hue. Playtest question. |
| 3 | The single-column **pad test** needs a re-spec, not a re-point (Proving Grounds has one Health pad; a consumed pad drops below `isPadTeal`) | **Ruled: re-spec the assertion to "the pad re-arms on its `everyTurns` cycle."** That tests the actual pad behaviour and is map-agnostic — better than coupling it to Iron Basin's pairs. Folded into RENDER-SUITE-GREEN-4. |
| 4 | **VFX-FLASH-ON-SCREEN** measures a spike that is not there (flat lit-pixel counts; a step change in scene brightness, not one victim lighting) — *may be a real VFX regression* | **Spec VFX-FLASH-VERIFY (reproduction-first, potential real bug).** The flash's decision/delivery/material/paint are each unit-tested, but "unoccluded on screen" is exactly what RENDER-VERIFY is for, and the measurement says no localised spike. Diagnose whether the flash lands and is visible (a real regression — occlusion, or the paint not reaching the lit mesh) **or** the test measures global brightness instead of a local spike; fix whichever. A hit that doesn't read as a hit is a core-feel bug, so this leads the next session. |
| 5 | No engine work this session | **Acknowledged** — confirmed by the empty `packages/engine/src` diff. |

## Issues, ranked

### 1. [MED — possible real bug] The victim flash may not be reaching the screen (OQ #4)
`e2e/vfx.spec.ts:71`: best spike 165 against a floor of 800, counts flat across the resolution. Not
obviously a coordinate/colour drift like its neighbours. **RENDER-SUITE-GREEN-4 does not absorb this** — it
gets its own reproduction-first item, VFX-FLASH-VERIFY, because it might be the flash, not the test.

### 2. [MED — test infra] Two render tests need a drive, not a re-point (OQ #3, FOG-ZORDER)
The pad-pair test and FOG-ZORDER's aimed half are "a suite written against a board that framed everything,
asked to work on one where the camera and the map both moved." FOG-ZORDER's `bestAimed` moved 0→1 (floor 20)
because the nearest lit brush sits at the very edge of a turn-1 reach and most brush is fogged on the opening
frame — so no candidate choice fixes it; it needs to **drive** a character toward a brush band, then measure.
**RENDER-SUITE-GREEN-4** carries both re-specs.

## Next Builder session

**VFX-FLASH-VERIFY → RENDER-SUITE-GREEN-4 → FOF-OUTLINE.** VFX-FLASH-VERIFY leads because it may be a real
hit-feel bug, not test drift; RENDER-SUITE-GREEN-4 closes the last render-suite tests with the re-specs ruled
above; FOF-OUTLINE completes the owner's FoF ask via the rim-light tint if the playtest wants more than
ring+nameplate. FOF-OVERLAY-HUE is a Designer flag, not a Builder item until the hue is chosen. Standing
**PLAYTEST** continues. Full AC + Spec Notes in BACKLOG. PR to `main` per the standing instruction.
