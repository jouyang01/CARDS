# Review — 2026-09-29 (Analyzer)

Scope: **PR #105** (Builder session 11 — **RAM-PREVIEW-REVERT**, **WALL-HIT-ONCE**, the **TTK package**:
HP band, tiered skill damage, Lumen's heal, 2v2 turn limit) and the Builder's eight session-11 Open
Questions. Base: `origin/main` (`b48e515`). **Two Dev Notes** — the art-pipeline reference, and the
green light to **start playtesting**.

## Verification

- `npm test` → **2644 passing** (1366 + 976 + 302). `npm run typecheck` clean. Engine purity grep clean.
- **TTK — every shipped number matches the spec.** HP band exact (`wisp 100, kestrel 105, cinder 110,
  vex 110, thorn 130, lumen 140, aegis 155, bastion 170, ravok 175`); the eleven skill values spot-checked
  (Skim 30, Frag 33, Chain Hook 23, Bullrush 17, Flare Burst 12 — all match); `mending_light` heal 20,
  cooldown still 2; `FORMATS['2v2'].turnLimit` 20, `killsToWin` 4, 4v4/1v1 untouched. **TTK-INVARIANT**
  (`content.test.ts`) enforces the HP ladder, the archetype ordering (firepower < support < frontline),
  and the tier formula `round_half_up(basic × shape × rider × delay)` **computed from each ability's own
  fields**, so a tenth character is checked by the same rule — the right shape for a load-bearing invariant.
- **WALL-HIT-ONCE — sound, N-safe, phase-order intact.** The dedup set (`trapHits`, keyed
  `unitId|groupId`) is **threaded** through the thirteen trap-trigger callers rather than stored on
  `GameState` (correct — one-resolution scratch; storing it would ship per-turn bookkeeping to every
  client and saved room and invite a clear-bug for a future `lifetime: 2` `perTile` hazard). `groupId` is
  stamped **only** on `perTile` tiles and is the trap id minus the square (the *cast*, not the tile), so
  two walls in a turn stay distinct. The trigger check adds one keyed-set lookup — no float, no RNG, no
  change to Prep→Dash→Blast→Move or dash immunity. Verified against the ruling.
- **RAM-PREVIEW-REVERT — clean.** `tessellate`'s `path` case returns `[]` again (the charge is
  deliberately bare), with a four-line comment preserving *why* a charge is unlike every other shape (its
  locus is a route, not `aim[0]`). The Builder correctly found the Spec Notes' third file
  (`app.ts`) needed no change — its shape-layer call is generic — and said so; not an omission.

## Done well (one line)

The Builder shipped the TTK package as **one green commit** against the "commit per item" habit and was
right to: the five items are interdependent (HP alone reddens twelve tests that name old numbers), so
"one green commit beats five where four cannot be bisected" — and it updated ten ability descriptions so
no tooltip reads "12 damage" beside a 30.

## Builder Open Questions — session-11 resolution (#1–8)

| # | Question | Resolution |
|---|---|---|
| 1 | First-only charge preview (one number, not one per enemy) is the one bit of "before" not restored | **Confirmed correct — keep it.** "Go back to how it was before" was about the *lane outline*; restoring the pre-fix per-enemy numbers would restore a preview that **lies** (Skim showing damage on enemies a first-only charge cannot hit). The number-correctness fix stays. The owner has not objected across two PRs. Closed. |
| 2 | `groupId` is a new `TrapState` field, therefore protocol-visible, added without a protocol item | **Ruled acceptable** (edge-cases, WALL-HIT-ONCE): benign additive field like `halt`/`triggers`/`expiresOnTurn`; nothing renders it and the state contract need not enumerate it. Flagged only for a future `ARCHITECTURE.md` state-contract pass. |
| 3 | WALL-HIT-ONCE weakens the wall (75→25 along its length); WALL-REACH (~7 tiles) partly justified by that — judge together | **Folded into the PLAYTEST checklist.** The wall's power is now one question (reach **and** one-hit-per-unit), not two separate nerf/keep calls. Designer/playtest. |
| 4 | The cross-phase AC example is vacuous as written (straight dash-then-shove re-hits the consumed tile) | **Ratified the Builder's fix; re-specced the bullet** (edge-cases, WALL-HIT-ONCE): a real cross-phase test needs a **lane-changing** victim (charge, bend off, hook back across a *different* tile). Recorded so the next reader doesn't write the vacuous one. |
| 5 | TTK-INVARIANT makes the tier rule load-bearing — a deliberate outlier now needs a named exception | **Confirmed intended; ruled** (edge-cases, TTK-INVARIANT): an intentional outlier gets a **named exception** in `content.test.ts` with a one-line reason (as traps and Aegis already are); a *silent* outlier is the failure, not an outlier as such. |
| 6 | `roster-v1.md` §4 ceilings stale in four places | **Kept as a Designer flag** (backlog): undelayed skill cap 24→30, nuke ceiling 34→33, sustain 25→20, "4–5 hits on 100 HP" → ~5.9 on 100–175. TTK-INVARIANT enforces the live numbers meanwhile. |
| 7 | `GAME_SPEC.md:119` still says `maxHp` baseline ~100; median is 130 now | **Routed as a one-line doc-fix** (backlog GAMESPEC-HP-UPDATE). A factual staleness in the ruleset doc; the Builder rightly didn't widen a docs edit its AC didn't name. |
| 8 | PLAYTEST is unblocked | **Confirmed — and greenlit by Dev Note #2.** Nothing in the Builder backlog blocks it. See §Dev Notes and the backlog's PLAYTEST milestone. |

## Dev Notes routed (verbatim)

1. *"Make sure the anazlyer and builder can see the art pipeline.md file and PR #100 for art/animation/VFX
   reference while building and speccing."* → **`docs/ART_PIPELINE.md` is present (335 lines, with a §8
   role brief for each of Builder/Analyzer/Designer) and already in CLAUDE.md's repo map; PR #100 is
   merged on `main`.** Both are accessible now. Routed into the backlog as a **standing reference block**
   so every future art/animation/VFX item points at both, plus a flagged-future **ASSET-WEIGHT-BUDGET**
   item (the doc's own Analyzer brief names it as "a good first backlog item") — see backlog.
2. *"We should start playtesting."* → **PLAYTEST is now the active GO milestone** with a concrete
   watch-checklist (below and in the backlog), folding in the TTK questions, the wall power (OQ #3), and
   the least-tested paths. See backlog.

## Where things stand, and what to watch in the playtest

The whole of Path A's pre-playtest work is done: the two client bugs, the two ability fixes, and the TTK
tuning are all in and verified. The game is deployed and, by the suite, correct. **Start playtesting.**
The one Builder deliverable that pairs with a *networked* playtest is **NET-E2E** — automated two-client
coverage of the loop — because the networked relay is exactly the untested seam (DEATH-HANG was one such
bug; session-9 OQ #4's rotated-wall relay is still "verified by reading the protocol"). It is specced in
the backlog to run alongside the owner's playtest, not to block it. The playtest checklist and the small
doc-debt items (GAME_SPEC HP, roster-v1 §4) are in the backlog. PR to `main` per the standing instruction.
