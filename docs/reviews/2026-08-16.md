# Review — 2026-08-16 (Analyzer)

Scope: the T1 rename + M2 interactive-client work now on `main` (PR #6,
`5285f18…69e982c`): `PlayerId → TeamId` rename, decision-phase targeting logic (18),
event-log playback (19), hot-seat seats + per-player order merging (20), the interactive
shell (ce238a5), and a client Vitest runner. Base: `origin/main` (`1bca7ed`) — the
integration point for all engine + client + roster work.

Reconciliation note: `main` did not carry my 2026-08-15 review or its deduped
BACKLOG/edge-cases (branch `6eb9552` was never merged). This review branch now = `main`'s
code + those docs brought forward; `main`'s DECISIONS (with the Builder's Open Questions)
is kept as-is.

## Verification

- `npm test` runs **both workspaces** now: engine **226** + client **18** = **244
  passing**. `npm run typecheck` clean (engine + client); `npm run build` produces the
  client bundle.
- **Purity/determinism holds.** No RNG/clock/`performance.now`/float literals/float-math
  in `packages/engine/src`; the T1 rename introduced nothing impure.
- **T1 is complete, not aliased** — `grep PlayerId packages/` returns nothing;
  `PlayerOrders.player → .team`; all 226 engine tests pass unchanged (pure mechanical
  rename, no behavior diff). Correct isolation.
- **Phase order + dash immunity unaffected** by the rename or client work.
- **Client is a pure `TurnEvent[]` consumer (Dev Note 1 honored).** `playback.applyEvent`
  folds only stated deltas (moveStep/damage/heal/energyGained/death/respawn/gameEnd), never
  recomputes; `targeting.ts` delegates all legality to engine helpers (`expandShape`,
  `aimInRange`, `validateMovePath`, `movementBudget`, `reachableSquares`); `hotseat.ts`
  merges per-seat orders into two `PlayerOrders` — the engine still sees two teams.
- **N-unit-safe.** `deriveSeats` splits a team ≤2 per player (3-player 2v2 = 1+1 vs 2; 4v4
  = 2+1+1); playback/render loop `state.units`. No single-unit shortcuts.
- **Golden rule #3 holds** — the client's AC-bearing pure logic ships with tests
  (targeting 9, playback 4, hotseat 5).

## Done well (one line)

The interactive client landed as a strict event-log consumer with its own tested pure
core — targeting, playback, and seat-merging are all unit-tested without a DOM, and the
DOM shell stays a thin typecheck/build-verified skin.

## Issues, ranked

### 1. [Medium — schema gap, blocks full HUD] Event log can't reconstruct shields / post-ult energy
`packages/engine/src/types.ts:225` (`statusApplied` has no `amount`) and the absence of an
energy-spend event mean `playback.ts` reproduces the board exactly but not shield pools or
post-ultimate energy (the Builder flagged and did NOT work around this — correct call).
**Fix: backlog item S1** — add `amount?` to `statusApplied` (shield pool) and an
`energySpent` event (ult reset), each with tests; then the playback HUD consumes them.
Ruled in edge-cases ("Rendering contract").

### 2. [Low — carried, correctness direction] Movement diverges from the intended AR model
Per Dev Notes 1 & 2, movement should let units (and dashes) pass *through* other
characters; today enemies block pass-through and a walked charge stops in front of the
first unit. Not a bug against the *current* spec, but a spec change the human wants.
**Fix: backlog items MV1 (dashes through characters) + MV2 (movement → AR model)**; PROPOSED
rulings added to edge-cases. The AR wiki was egress-blocked this session, so the exact
edge-details need verification (noted in the ruling).

### 3. [Low — constitution drift] `CLAUDE.md` still says "npm test — engine test suite"
Root `npm test` now runs engine **and** client workspaces (the Builder's intended shape —
confirmed good). `CLAUDE.md`'s Commands section is now slightly stale. I did not edit the
shared constitution unilaterally; recommend a one-line update to "engine + client suites"
(cross-role note in the backlog).

### 4. [Low — process] `main` engine trails resolved; branches to retire
PR #6 merged the engine/client work to `main`, so the earlier "main trails" issue is
closed. Stale branches (`multiplayer-configs`, `engine-backlog-t96lxw`) can be retired.
**Do not retire `code-review-h3mwjs` yet** — it still carries un-merged review docs
(this review + the 08-15 reconciliation); merge those to `main` first.

### 5. [Low — coverage gap] Interactive DOM shell has no runtime test
`app.ts`/`render.ts` are typecheck/build-verified only (no Playwright in-repo). Acceptable
— the pure logic beneath is covered. **Fix: backlog item C1 (optional)** — a headless
one-turn smoke test when tooling is available.

No correctness defects found in the shipped rename or client logic.

## Builder Open Questions — resolution

| Question (2026-08-15) | Resolution |
|---|---|
| Event-schema gaps for playback (shield amount; ult energy reset) | **Ruled** (edge-cases "Rendering contract") → **item S1**: add `statusApplied.amount` + `energySpent` event, with tests; client then consumes them. |
| Client test runner / root `npm test` runs both workspaces + CI | **Confirmed** desired — client has AC-bearing pure logic worth testing, and CI already runs `npm test` (both) + typecheck + build. Flag: update `CLAUDE.md`'s "engine test suite" wording (issue 3). |
| Main PR + retire stale branches | **PR #6 merged** (main = 1bca7ed). Retire `multiplayer-configs` + `engine-backlog`; **keep `code-review` until its docs merge** (issue 4). |
| Interactive shell not runtime-tested | **Acknowledged** → optional headless smoke test, **item C1**. |
| Designer follow-ups (decoy, combat_roll, cover-vs-Might, duplicate-pick, roster §9 ENGINE ASKs) | Carried in backlog's blocked/Designer section; **do not implement in v1 without a ruling** (esp. roster §9). |

## Next Builder session

Movement is the human's priority this session (Dev Notes). Build in order: **S1 (event
schema for shields/energy — small, unblocks the HUD) → MV1 (dashes pass through characters)
→ MV2 (movement → AR model: pass through anyone, never end on an occupied square) → TT1
(ability tooltips) → C1 (optional headless smoke test)**. MV1/MV2 carry the AR-verification
caveat; the damage-on-charge rule is an ENGINE ASK held for the Designer. Then PR this
review branch's docs to `main` and retire the stale branches.
