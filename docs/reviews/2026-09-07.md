# Review — 2026-09-07 (Analyzer)

Scope: PR #56 (`925cfe9…05269cc`) — the combat-correctness batch (LOS-OCCLUSION, MELEE-COVER,
CHASE-SPRINT, MOVE-FOG, STATUS-ICONS-SIZE, ANIM-SLOW, PADS-LIGHTS, PADS-SCHEDULE, RENDERER-SPLIT,
RENDER-DRIVE-FIX) — and PR #57 (`05269cc…655f392`) — the Designer's nameplate-layout revision +
Might-to-centre pad placement. Base: `origin/main` (`925cfe9`, my 2026-09-06 review). Two Dev Notes:
a Vex first-sprint bug, and "address PR #57."

## Verification

- `npm test` → **1420 passing** (engine 669 + client 627 + server 124). `npm run typecheck` clean;
  build green. Engine purity/determinism intact — no RNG/clock/trig/`sqrt` in game math. Phase order
  + dash immunity untouched.
- **LOS-OCCLUSION reuses `hasLineOfSight`** rather than a new occlusion routine — line/cone tiles
  are filtered through the engine's own sight kernel, so "walls block, cover only reduces" is **one
  rule for sight and for shapes** (they cannot drift apart), integer/deterministic (no-trig guard
  holds), `circle`/`square` still lobbed. The HITBOX1 golden signature **correctly did not move**
  (its fixture is a wall-free `openBoard(41)` — nothing to occlude; forcing it would change what
  should not change). ✔ (OQ #4).
- **MOVE-FOG filters the STATE** (`planningState(state, fogView().units)`), the same move the server
  makes for M3-HIDDEN — no "should I hide this" branch to get wrong — applied to the reach envelope,
  the drawn route, the chase route **and the committed path**. Engine untouched; contact is the
  reveal. Structurally sound.
- **CHASE-SPRINT derives the budget** (`chaseSprints`, a thin alias of `sprintAllowed`) rather than
  reading `order.sprint` — a chase's route is picked at end of Move, so there's nothing to opt into,
  and the condition is Sprint's own (no ability, no Dash catalyst; a free action doesn't block).
- **PR #57 pad placement verified in data** — Might → central (7,7)/(10,7) & (9,9)/(12,9),
  Health/Energy → flanks (`firstTurn: 4`); both maps still pass the PADS1 mirror + PADS-SPREAD +
  content guards. Resolves the Might-contestability handoff (OQ #2).

## Done well (one line)

MOVE-FOG closed the hidden-info leak by filtering the *state* the planner sees rather than
special-casing each query — the identical structural move M3-HIDDEN makes — so the reach envelope,
the drawn route, and the **committed order** are all planned against the same fogged board and a
preview can never disagree with the order it turns into.

## Issues, ranked

### 1. [HIGH — bug, reproduce empirically FIRST] Vex's first sprint does not move (Dev Note #1)
*"BUG: Vex's first sprint action does not move the character."* I traced the sprint path statically
and it **reads correct** at every step: `selectSprint` sets `sprint: true` + clears the ability
(`targeting.ts:887`), `sprintAllowed` is `abilityId === undefined && !dashCatalystArmed` (true for a
fresh Vex), and `onBoardClick`'s move branch commits `pathTo(map, planningState(...), unit, sq,
movementBudget(unit, draft.sprint))` with `sprint = true` → budget 8 (`app.ts:1356`). Nothing I can
see returns an empty path or a 4-budget on the first sprint. **So this needs EMPIRICAL reproduction,
not more reasoning** — the exact lesson from the UI1 saga (a "still broken" report whose root cause
was a stale cached bundle, not a code defect; review 2026-08-24/25). **Fix: backlog
MOVE-SPRINT-FIRST** — the Builder must (a) reproduce in the running hot-seat, (b) confirm it is code
vs. a stale build, then (c) fix + ship a client regression test. **Prime suspect: MOVE-FOG** (the
newest movement change; the committed-path `planningState` on turn 1). Rank it HIGH — a first-action
movement bug blocks every playtest.

### 2. [MED — rule inert in content] MELEE-COVER ships with no ability marked (Builder OQ #3)
The `melee: true` flag is wired, but **no shipped ability carries it** (verified: no `"melee"` in
`data/`), so every attack still eats cover. That is the intended sequencing (the data pass is the
Designer's), but the rule is inert until it lands. **Fix: route the melee-ability data pass to the
Designer** (mark the short-range strikers). Recorded in the Designer routing block. Confirmed, not a
gap.

### 3. [LOW — verify] Confirm the chase e2e is green (Builder OQ #5)
RENDER-DRIVE-FIX got both drives to contact (LAST-KNOWN passes); the chase drive then failed on its
own assertion — clicking the *median* of red pixels landed between two now-visible bodies — and the
Builder added `largestCluster` to `pixels.ts`. **Verify the chase e2e is now green** (I can't run
Playwright here); if the `largestCluster` fix didn't fully land, it's a small RENDER-DRIVE-FIX
follow-up. Also worth noting the Builder's flag: **anything that "finds a unit by its pixels" has the
same median-between-bodies bug latent** — use `largestCluster`.

### 4. [MED — from PR #57] The nameplate needs its layout revision (Dev Note #2)
PR #57 specs **NAMEPLATE-LAYOUT** (name left-justified, status row beside the name, **buffs BLUE /
debuffs RED** per the FF1 polarity table). It supersedes STATUS-ICONS-SIZE's wrap-at-six layout
(Builder OQ #6) — "one repaint, not two." **Fix: backlog NAMEPLATE-LAYOUT** (client). The other two
PR #57 items need no build: **PADS-PLACEMENT** shipped in data (verified), **health-pad parity**
confirmed.

No correctness or determinism defects in the shipped code.

## Builder Open Questions — resolution (2026-09-07 #1–8)

| # | Question | Resolution |
|---|---|---|
| 1 | PADS-SCHEDULE implemented though routed to Designer | **Accepted** — Dev Note #6 was pasted into the Builder's session (Builder scope); a two-field data change. PR #57 then moved the pads keeping the schedule — no conflict. |
| 2 | Might contestable is half-satisfied (placement is the lever) | **Resolved by PR #57** — the Designer moved Might to the central strongpoint. Ruled in edge-cases (PADS-PLACEMENT). |
| 3 | MELEE-COVER inert until abilities marked | **Confirmed intended** — the melee data pass is the Designer's (issue 2); routed. |
| 4 | LOS-OCCLUSION golden signature did not move | **Accepted** — wall-free fixture, nothing to regenerate; the AC's expectation was wrong, the code is right. |
| 5 | RENDER-DRIVE-FIX needed `largestCluster` | **Acknowledged** — good catch; **verify the chase e2e is green** (issue 3), and the median-pixel bug is latent elsewhere. |
| 6 | STATUS-ICONS-SIZE forced a wrap-at-six layout | **Superseded by NAMEPLATE-LAYOUT** (PR #57 revises the whole layout) — folds in there, one repaint. |
| 7 | `MS_PER_BEAT` moved to `animate.ts` (now public) | **Accepted** — trivial, correct (lets the ANIM-SLOW test pin it without pulling in Three). |
| 8 | No real Workers runtime | **M3-DEPLOY** smoke check (unchanged). |

## Dev Notes routed (verbatim → backlog)

1. *"BUG: Vex's first sprint action does not move the character."* → **MOVE-SPRINT-FIRST** (client,
   HIGH — reproduce empirically first; prime suspect MOVE-FOG).
2. *"Make sure to address pull #57 from the designer."* → **NAMEPLATE-LAYOUT** (client, from PR #57
   §4.8); **PADS-PLACEMENT** recorded/verified (data, done); health-pad parity confirmed (nothing
   to build).

## Next Builder session

Bug first, then the Designer's UI revision, then M3. Order: **MOVE-SPRINT-FIRST** (HIGH — reproduce
in the running client *before* touching code; add a regression test) → **NAMEPLATE-LAYOUT** (client,
PR #57 — name left, icons beside name, blue/red polarity; folds STATUS-ICONS-SIZE's wrap) →
(verify/close the **chase e2e**) → **M3-LOBBY** (the large, multi-session network client — proves
M3-HIDDEN end-to-end, deletes the `POST …/start` route, R3 duplicate-pick, catalyst/character
selection). **Routed to the Designer:** the `melee: true` ability data pass (MELEE-COVER is inert
until it lands). Roadmap after M3-LOBBY: **M3-TIMER → M3-RECONNECT → M3-DEPLOY.** **Do not touch
vision** (per-format `visionRange` superseded by ar-parity §3); **remove the start route at
M3-DEPLOY.** Full AC + Spec Notes in BACKLOG. PR to `main` per the standing instruction.
