# Review — 2026-08-19 (Analyzer)

Scope: PR #12 (`de4012b…206a36d`) — MS1 (move **and** shoot in one turn, client), MV4
(diagonal charge paths) — and the Designer's `docs/design/rulings-v1-blockers.md` (R1–R7),
which closes the seven long-blocked items. Base: `origin/main` (`76081d3`; PR #13 also
re-merged the roster). My 08-17/08-18 reviews are now on main (PR #11).

## Verification

- `npm test` → **266 passing** (engine 240 + client 26). `npm run typecheck` clean; build
  green. Purity/determinism intact — no RNG/clock/`performance.now`/float literals in the
  engine.
- **MS1** — the engine already supported ability + move; the client now exposes it. Coverage
  is at the pure boundary (`toUnitOrders` round-trip + `movePreview` at the 4-budget). Phase
  order/dash immunity untouched.
- **MV4** — diagonal charge paths validate through `validateMovePath`'s adjacency +
  corner-cut rules and resolve; `walkCharge` still strikes `firstEnemy` (R1a). Deterministic.
- **Golden rule #3 holds** — MS1 and MV4 shipped with tests.

## Designer rulings folded into edge-cases.md (my main task this session)

All seven R1–R7 rulings from `rulings-v1-blockers.md` are now merged into `edge-cases.md`
(the ⚠ pending banner is replaced with a "folded in" note; the source file stays as the
Designer's rationale of record). Superseded/OPEN entries updated: the interim amount-1
knockback → **R1c carry-through**; the decoy no-op → **R2 full entity**; OPEN duplicate
picks → **R3**; the cover-vs-Might flag → **R5 final**; `untargetable` added to the polarity
table (**R7**, now total); the walked-dash ruling updated for MV1 pass-through + **R1a/R4**.

## Done well (one line)

The Designer file was written to be lifted verbatim with explicit `ENGINE ASK`s and
interim-safety notes — folding it in was mechanical, and every ruling that merely confirmed
shipped behavior said so, so no phantom work got scheduled.

## Issues, ranked

### 1. [Medium — test will break under R1c] A shipped test asserts the superseded interim
`packages/engine/test/dash.test.ts:182` asserts the amount-1 charge victim stays at `(2,2)`
("1-square knockback onto the charger nets zero (MV1-fix interim)"). **R1c supersedes this**
— the victim is carried past the charger to `(4,4)`. The Builder must **update this
assertion** when implementing R1c (the Designer flagged it too). Called out in backlog R1c.

### 2. [Low — interim, safe] Kestrel's Tempest Run under-performs until R1b
`chargeHits` is data-only (`kestrel.json` carries `"all"`; engine ignores unknown keys), so
the ult hits only the first enemy crossed — weaker than designed, never stronger. Safe to
ship; **backlog R1b** adds the field.

### 3. [Low — flag, Builder-raised] MS1 has no DOM-level test
The move/shoot toggle logic lives in `app.ts` (a DOM closure); only the pure boundary is
tested. **Optional backlog MS1-test** — extract a pure `nextDraft(action, draft)` reducer
from `selectAbility`/`selectMove` and unit-test it. Cosmetic sibling: MS1's move and ability
previews share the `--aim` color (Builder flag) — harmless.

No correctness defects in the shipped MS1/MV4.

## Builder Open Questions — resolution

| Question (2026-08-18) | Resolution |
|---|---|
| MS1 has no DOM test; offer a `nextDraft` reducer extraction | **Scheduled optional** — backlog **MS1-test** (do it if capacity; the toggle is worth a pure test). |
| CL1 (AR clash pass-through co-occupancy) — promote to RULED? | **Deferred for v1 — do NOT build.** The stricter same-step-stop is deterministic and no playtest demands AR-exact clashes. Stays PROPOSED; revisit only on a concrete complaint. |
| CL2 (vector-sum displacement) scope | **Deferred for v1.** Its main motivation (the Ram Charge net-zero) is now resolved by **R1c**; no other v1 driver. |
| Carried Designer-blocked (charge combat, decoy, dup picks, combat_roll, cover-vs-Might, Support, roster §9) | **All RULED and folded** into edge-cases this session (R1–R7); engine work scheduled (R1c, R1b, D1). |

## Next Builder session

Engine work created by the Designer rulings, smallest first: **R1c (displacement skips the
displacer's square — small; UPDATE `dash.test.ts:182`) → R1b (`chargeHits` field + implement
`"all"` + validation + content test) → D1 (decoy entity: `decoys: DecoyState[]` on
`GameState`, spawn/expire/destroy, `decoyDestroyed` event — the largest)**. Bundle the two
tiny content guardrails (R4: no `path` ability resolves as teleport; R7: every `EFFECT_KIND`
in exactly one polarity row). Optional: **MS1-test**. **CL1/CL2/E2 stay deferred.** Support
(R6) needs no engine work. A PR to `main` will be opened per the standing instruction.
