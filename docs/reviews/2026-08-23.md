# Review — 2026-08-23 (Analyzer)

Scope: PR #21 (`29850d3…7bc9a38`) — FF1-charge, FF1-delayed, MET1-tp, BRUSH1, the A1/A2/A3
re-spec (cue-driven tweened playback, spotlight, free orbit), BUNDLE1, and the ARCHITECTURE
SVG→3D finish. Base: `origin/main` (`291c003`). Also turns the owner's six UI notes (UI1–UI6)
into backlog items.

## Verification

- `npm test` → **399 passing** (engine 316 + client 83). `npm run typecheck` clean; build green
  (CI now fails >300 kB gzipped — BUNDLE1). Purity intact — no RNG/clock/trig/floats in the
  engine; still dependency-free.
- **FF1 is now complete** — `walkCharge` strikes the first **unit** crossed (`chargeHits:"all"`
  → all units); `detonateDelayedBlasts` applies harmful to all units in-area (beneficial stay
  own-team). Confirmed both.
- **MET1-tp** — teleport-strike is Manhattan-1; **BRUSH1** — brush locked as a legal
  dash/teleport/move destination end-to-end.
- **skip == watch holds by construction** — the animation `Frame` carries positions/fades but
  **no HP/aliveness/kills** (a test pins the `Frame` key set), so the board can only come from
  folding events; dropping every animated frame lands on the identical `ViewState`.
- **Phase order + dash immunity untouched.** ARCHITECTURE fully updated for the 3D renderer
  (my prior flag resolved).

## Done well (one line)

The readability batch was verified with a scripted browser run + composited screenshots — which
caught a real `transparent/needsUpdate` renderer bug no unit test would have found — while the
skip==watch invariant is defended structurally by keeping the board out of the animation layer.

## Issues, ranked

### 1. [Medium — scope requirement, not a defect] A0 must extend to `heal` + `statusApplied` for UI6
The combat log (UI6) shows healing "to and FROM characters", but `heal` (`types.ts:264`) and
`statusApplied` (`:267`) carry no source — only `damage` got A0's `sourceUnitId`/`abilityId`. So
"Aegis shielded Lumen for 30" isn't expressible from the log. **Fix: backlog A0-heal** — add the
two fields to `heal` and `statusApplied` (purely additive, no outcome change), the same treatment
`damage` already has. Blocks UI6; do first.

### 2. [Low — deferred, now feasible] RND1 render output still not in automated CI
The Builder verified this batch by hand (screenshots) and caught a bug that way. The env has
Chromium + Playwright available, so a headless smoke test is now feasible. **Fix: optional backlog
RENDER-VERIFY** — a Playwright devDependency + one CI job driving a scripted turn and asserting a
few composited pixels. Not blocking; the renderer's *inputs* remain unit-covered.

No correctness defects in the shipped FF1-charge/delayed/MET1-tp/BRUSH1/A1-A3 re-spec.

## Builder Open Questions — resolution

| Question (readability batch) | Resolution |
|---|---|
| `MS_PER_BEAT = 460` pacing (never played) | **Playtest tunable** — one number in `app.ts`; turn it if 4v4 reads long. No action. |
| Bars billboarded but unlabelled (no name/HP text on board) | **Defer — bars only for now.** On-board name/HP text is in the "observed-not-requested" scope fence; if wanted later, DOM overlays tracked to the camera. |
| Spotlight dims off-actor bars (can't watch their HP mid-phase) | **Acceptable** — UI5 readouts + the UI6 log cover mid-phase damage; if playtest wants it, keep bars lit while dimming the body (the cheap fix). |
| Free orbit resets on projection change | **Acceptable default** (presets exist to re-home the camera); one-line change if players expect yaw kept — revisit at playtest. |
| `objectFor()` on `Renderer` is now unused | **Keep it** — it's the natural seam for A4's per-ability FX. Don't delete. |
| RND1 render verification via Playwright | **Yes, schedule it** (optional **RENDER-VERIFY**) — it already caught a real bug; env has Playwright/Chromium. |
| Designer/data items (M1, M1-4v4, Thorn-dash) | Unblocked, waiting on the Designer — unchanged. |

## Owner UI batch (UI1–UI6) — routed into BACKLOG

Reconciled with what already shipped (A0/MET1/AIM2/RND1/A1–A3 are **done**, so the owner's
"revised sequencing" preamble is largely already built):
- **A0-heal** (engine, first) — widens A0 to `heal`+`statusApplied` (required by UI6).
- **UI1** hover-range preview + click-to-commit + commit≠lock (subsumes AIM2-UX; screen-space).
  **Lock-In ruling:** Lock In commits the **currently-selected character**; a player switches
  freely between their 1–2 characters until all are locked; committing an action does **not** end
  the turn.
- **UI3** HUD layout (bottom-left portrait/HP/energy · centre hotbar · right Lock In; persistent
  DOM; screen-space; depends on UI1's ruling).
- **UI6** scrollable combat log (screen-space; depends on A0-heal).
- **AIM1 (+UI4)** movement line + endpoint marker; **dash uses the same line in YELLOW**
  (world-space).
- **UI2** two-layer overlay — continuous shape (fiction) + affected tiles (truth) — now unblocked
  (AIM2 centre-in-binary ruling + RND1 both landed; world-space).
- **UI5** damage/shield-absorb/heal readouts during resolution (folds into A3; world-space anchor).

**Observed-not-requested (recorded, NOT scoped):** turn timer, score/objective header, per-unit
name labels, status icons — from the reference screenshot; owner did not request them.

## Next Builder session

Engine first: **A0-heal**. Then screen-space (renderer-agnostic, parallel): **UI1 → UI3, UI6**.
World-space (renderer done): **AIM1(+UI4), UI2, UI5**. Designer/data (**M1, M1-4v4, Thorn-dash**)
in parallel. **RENDER-VERIFY** optional; **A4/CL1/CL2/E2** deferred. Full AC + Spec Notes in
BACKLOG. PR to `main` per the standing instruction.
